{% extends "main.html" %}

{% block content %}
<table>
    <tr>
        <th>Team Name</th>
        <th>Team ID</th>
    </tr>
    {% for i in teams %}
    <tr>
        <td><a href="{{url_for('past_chat_select', team_id=i[0])}}">{{i[1]}}</a></td>
        <td>{{i[0]}}</td>
    </tr>
    {% endfor %}
</table>
{% endblock %}